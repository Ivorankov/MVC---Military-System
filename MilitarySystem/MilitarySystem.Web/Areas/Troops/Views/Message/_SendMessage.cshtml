@model MilitarySystem.Web.Areas.Troops.ViewModels.MessageIndexViewModel
@{

}

<div class="row ui-widget-content" id="message-details-box">
    <div class="col s12 m12">
        <div class="card grey lighten-5 center-align black-text human-text floating-box">
            <br />
            <form id="__AjaxAntiForgeryForm" action="" method="post">
                @Html.AntiForgeryToken()
                <strong class="blue-text">@ViewBag.HeaderText</strong>
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div class="floating-box">
                    @Html.LabelFor(m => m.SendInput.Content)
                    @Html.TextAreaFor(m => m.SendInput.Content, new { @cols = "80", @rows = "10", @class = "text-area-messanger", @id = "message-to-send-content" })
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <button class="btn waves-effect waves-light blue" id="send-message-btn-test">Send</button>
                    </div>
                </div>
            </form>

            <strong>Recived messages</strong>
            <div class="row">
                <div class="col col-sm-8 m8 test">
                    <br />
                    <div id="message-content" class="message-details black-text">

                    </div>
                </div>
                <div class="col col-sm-4 m4  center-align text-center">
                    <div id="message-grid" class="blue center-align text-center">
                        @foreach (var a in Model.MessageView.Content)
                        {
                            <div data-userId="@a.Content">@a.User.FirstName</div>
                                <br />
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
